# Playwright Python Tests

This folder contains a Python Playwright + pytest test suite mirroring the Java POM framework.

## Setup

From the repo root:

```bash
cd py-tests
pip install -r requirements.txt
python -m playwright install
```

## Run tests

```bash
cd py-tests
pytest
```
